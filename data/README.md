# Experiment Data

The full 30,000-row dataset is intentionally generated locally so the repository stays lightweight and reproducible.

Run:

```bash
python src/generate_experiment.py
```

This creates `pricing_experiment.csv` with visitor, device, country, variant, conversion, price and revenue fields.
